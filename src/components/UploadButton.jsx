import React from 'react';
import {Button, TextField} from "@mui/material";

const UploadButton = () => {
    return (
        <div>
            <TextField></TextField>
            <Button variant="contained" color="primary">Subir</Button>
        </div>
    );
};

export default UploadButton;